<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="5dp"
    android:layout_marginEnd="5dp"
    android:layout_marginBottom="14dp">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/card_book_cover"
        android:layout_width="0dp"
        android:layout_height="130dp"
        app:cardCornerRadius="14dp"
        app:cardElevation="0dp"
        app:strokeColor="#E8E8E8"
        app:strokeWidth="1dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <View
                android:id="@+id/view_thumb_bg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#F0EDFF" />

            <!-- 즐겨찾기 별 (우상단) -->
            <ImageView
                android:id="@+id/iv_favorite"
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:layout_marginTop="6dp"
                android:layout_marginEnd="6dp"
                android:padding="3dp"
                android:src="@android:drawable/star_off"
                app:tint="#CCCCCC"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- 편집 모드 체크박스 — 커스텀 ImageView로 확실히 표시 -->
            <ImageView
                android:id="@+id/iv_check_box"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_marginStart="7dp"
                android:layout_marginTop="7dp"
                android:src="@drawable/ic_check_off"
                android:visibility="gone"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- 숨겨진 실제 CheckBox (선택 상태만 관리용) -->
            <CheckBox
                android:id="@+id/cb_delete"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:visibility="gone" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/tv_book_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="2"
        android:text="예시"
        android:textColor="#222222"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@id/card_book_cover"
        app:layout_constraintStart_toStartOf="@id/card_book_cover"
        app:layout_constraintTop_toBottomOf="@id/card_book_cover" />

    <TextView
        android:id="@+id/tv_book_date"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center"
        android:textColor="#AAAAAA"
        android:textSize="10sp"
        app:layout_constraintEnd_toEndOf="@id/card_book_cover"
        app:layout_constraintStart_toStartOf="@id/card_book_cover"
        app:layout_constraintTop_toBottomOf="@id/tv_book_title" />

</androidx.constraintlayout.widget.ConstraintLayout>